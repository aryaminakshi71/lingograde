/**
 * Basic utility tests for LingoGrade
 */

import { describe, it, expect } from 'vitest';

describe('LingoGrade Utils', () => {
  it('should have basic test setup working', () => {
    expect(true).toBe(true);
  });

  it('should perform basic math operations', () => {
    expect(1 + 1).toBe(2);
    expect(2 * 3).toBe(6);
  });
});
